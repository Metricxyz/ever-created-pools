node_modules/
.env

# Ephemeral/regenerable pipeline outputs — except liquidity-holders.json, which the GitHub Pages
# site (docs/index.html) fetches directly, so it needs to be committed. Note `outputs/*`, not
# `outputs/` — a directory-level ignore prunes the whole tree before the negation below can apply.
outputs/*
!outputs/liquidity-holders.json
